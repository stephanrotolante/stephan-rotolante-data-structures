const myFunction = (value: string): string => {
  return value || "default value";
};
const myFunction2 = (value: string): string => {
  return value || "default value";
};
const myFunction3 = (value: string): string => {
  return value || "default value";
};
module.exports = {
  myFunction,
};
